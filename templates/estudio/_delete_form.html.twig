<form method="post" class="form-button-delete" action="{{ path('estudio_delete', {'id': estudio.id}) }}" onsubmit="return confirm('Estás seguro de que quieres borrar este registro?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estudio.id) }}">
    <button class="btn btn-danger">Borrar</button>
</form>
